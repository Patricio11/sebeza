# SYSTEM ROLE & CONTEXT
You are the Lead Full-Stack Architect and Product Engineer for **"Sebenza"** *(working name  SA talent/employment platform)*.
We are building a **national talent-intelligence platform**, not just a job board.

Our edge over the existing national talent registry is **three things only: data quality,
usability, and real-time employment analytics.** The incumbent registry is legally mandated
and free  but its work-seeker data is publicly known to be stale and unreliable. We win on
those three dimensions. We are NOT rebuilding that registry; we build the trustworthy,
real-time layer on top of it.

> **Tone rule (non-negotiable in product copy):** Never name the incumbent in user-facing
> copy. Never compare ourselves to it. Sebenza stands on its own merits  *what it is*, not
> *what something else isn't*. The strategic context above is for the team, never for the page.

**Vibe:** "Government-grade trust meets consumer-grade usability."
Clean, fast, authoritative, accessible. Think Stats SA dashboard credibility + a modern, friendly
profile experience. **NOT** flashy. (See Rule 1  this is deliberate, not an oversight.)

**Primary users:**
1. **Job seekers**  across the full income spectrum, often on low-end Android phones and expensive metered data.
2. **Employers / recruiters**  searching for talent by skill + location + employment status.
3. **Government / policy**  consuming employment analytics (the strategic wedge).

---

# COMPANION DOCUMENTS (read together)
- **`ROADMAP.md`**  the phased build plan (Phase 0 → deployment). *What* to build and in what order.
- **`UX_UI_SPEC.md`**  design system + Phase 1 screen-by-screen UX + the typed mock-data layer + expanded detail for Phases 2–6. *How it looks and behaves.*
- **`MOBILE_PLAN.md`**  mobile-responsiveness phases (M1–M7), all done. The No-Flash Rule made concrete at 360 px.
- **Phase completion docs** (in `docs/completed/`)  `PHASE_0_COMPLETE.md` · `PHASE_1_COMPLETE.md` · `PHASE_1_5_COMPLETE.md` · `PHASE_2_COMPLETE.md` · `PHASE_3_COMPLETE.md` · `PHASE_4_COMPLETE.md` · `PHASE_5_COMPLETE.md` · `PHASE_6_COMPLETE.md` · `PHASE_6_5_COMPLETE.md` + archived plan/smoke-test records.
- **`docs/SECURITY.md`**  three-layer security model (proxy = UX, DAL = real gate, Server Actions = defense-in-depth). Read before touching `lib/auth/*` or adding a new protected page.
- **Active phase plan**  `PHASE_N_PLAN.md` at the top of `docs/` for the phase being built. Today: `PHASE_10_LAUNCH_PLAN.md` (public-launch tasks 10.5  10.11, code-side prep landed; operator-hands tasks remain) **+** `PHASE_11_PLAN.md` umbrella with sub-phase plans `PHASE_11_{3,4,5}_PLAN.md` (Phase 11.1 + 11.2 shipped 2026-05-30  see `docs/completed/PHASE_11_{1,2}_COMPLETE.md`; next up is 11.3 seeker control + trust posture).
- **This file**  always-on context + non-negotiable rules. Paste it at the top of every session.

When I give you a Phase: pull design/screen detail from `UX_UI_SPEC.md` and task detail from `ROADMAP.md`.

> **Doc convention (non-negotiable when a phase ships):**
> 1. Write `docs/completed/PHASE_N_COMPLETE.md` (what shipped + verification).
> 2. Tick the phase header in `ROADMAP.md` with ✅ + date.
> 3. Update the **Current State** block below.
> 4. Open `docs/PHASE_(N+1)_PLAN.md` with the recheck for the next phase.
> 5. Commit with `Phase N complete + Phase N+1 opens` in the message.
>
> Active plans live at the top of `docs/`. Completed phases move into `docs/completed/`.

---

# CURRENT STATE (read this before doing anything)

- **Phase 0** (foundations + POPIA spine)  ✅ done 2026-05-21. See `docs/completed/PHASE_0_COMPLETE.md`.
- **Phase 1** (public face + search + redacted profile)  ✅ done 2026-05-21. See `docs/completed/PHASE_1_COMPLETE.md`.
- **Phase 1.5** (auth UI + seeker/employer/admin dashboards + Career compass + Student mode + Mzansi National + mobile pass + ESSA-positioning cleanup)  ✅ done 2026-05-22, mock-driven. See `docs/completed/PHASE_1_5_COMPLETE.md`.
- **Phase 2** (real Better Auth + consent persistence + audit-log persistence + session-based guards + full sign-up / sign-in / sign-out / verify / forgot / reset, with env-driven Mailtrap/Resend transport)  ✅ done 2026-05-22. **2FA enforcement deferred to Phase 7 task 7.2.** See `docs/completed/PHASE_2_COMPLETE.md`.
- **Phase 3** (profile CRUD via Server Actions + Supabase Storage uploads for CV/cert/photo + the time-aware employment-status engine with dashboard nudge banner)  ✅ done 2026-05-22. See `docs/completed/PHASE_3_COMPLETE.md`.
- **Phase 4** (Postgres FTS + ranking SQL + real `dbProvider` + signed photo URLs on public reads + ISR analytics)  ✅ done 2026-05-22. `SEBENZA_DATA_PROVIDER=db` is now the default. See `docs/completed/PHASE_4_COMPLETE.md`.
- **Phase 5** (employer dossier `/employer/dossier/[handle]` + audit-logged contact reveal + document download + Placement-Truth Rule with 30-day reveal gate + saved-searches / shortlists CRUD)  ✅ done 2026-05-23. See `docs/completed/PHASE_5_COMPLETE.md`.
- **Phase 6** (skills-gap engine + supply heatmap + freshness tiles + working CSV export + career compass on real demand + internship/graduate search filters)  ✅ done 2026-05-23. See `docs/completed/PHASE_6_COMPLETE.md`.
- **Phase 6.5** (side-phase polish: CSV injection guard + partial-match skills-gap + skill_gap_snapshots time-series + real `rankInPoolQuery` + skill-level demand + clickable heatmap + Δ deltas)  ✅ done 2026-05-23. See `docs/completed/PHASE_6_5_COMPLETE.md`. Strategic adds queued in `docs/PHASE_9_PLAN.md` (PDF / LMI / `/gov` route group / city granularity / forecast).
- **Phase 7** (admin actions live + moderation queue + suspended sign-in bounce + 2FA enforcement gated by `feature_flag_2fa_enforced` + in-app notifications with action-driven revalidate + per-kind preferences + audit-log filters + CSV export + public-surface polish: dynamic landing month / honest /search end-state / signed-in routing on `/p/[handle]` CTAs / DB-backed sign-up taxonomy / `/insights` consuming platform settings)  ✅ done 2026-05-23. See `docs/completed/PHASE_7_COMPLETE.md` and `docs/completed/PHASE_7_PLAN.md`. Every admin button is now real  zero dead controls.
- **Phase 7.5** (work-availability dimension decoupled from employment status + longitudinal education-to-employment analytics with k=10 primary + complementary suppression + dedicated `outcomes_research` opt-in consent + placement-source split for data quality, with Lever C contextual "Did you hire?" nudge on the dossier and a 12-person synthetic Wits BSc CS cohort seeded so /insights actually demos)  ✅ done 2026-05-23. See `docs/completed/PHASE_7_5_COMPLETE.md` and `docs/completed/PHASE_7_5_PLAN.md`. Compliance assertions exposed via admin-only `/api/admin/outcomes-compliance`.
- **Phase 8** (Resend transactional emails + 6 `CRON_SECRET`-guarded cron routes: 30-day hard-delete · `status.stale.warning` · `saved_search.new_matches` · nightly skill-gap snapshot · `outcome_snapshots` from 7.5.4 · `saqa-worker` · plus KYC + SAQA adapters, both **dormant behind admin-controlled `feature_flag_kyc_provider` / `feature_flag_saqa_worker`** until partnerships confirm. POPIA §23 data export + §24 self-erase shipped on `/dashboard/privacy`)  ✅ done 2026-05-23. See `docs/completed/PHASE_8_COMPLETE.md` + `docs/completed/PHASE_8_PLAN.md`. Operator runbook for flipping the gates is in the COMPLETE doc.
- **Phase 9** (launch readiness + government pitch  Privacy Policy + PAIA manual + cookie consent + 5-doc POPIA governance set in `docs/popia/` + CSP/HSTS/Permissions-Policy headers + Sentry skeleton (env-gated, no-op until DSN set) + rate-limit library (in-memory + Upstash-ready) **dormant by default** with the trade-off recorded in DPIA R8 + `loading.tsx` per route group + robots/sitemap/OG/canonical + Sebenza LMI with public `/api/lmi` + nightly snapshot cron + new `/gov` route group with `gov` role + provinces deep-dive + PDF print at `/insights/print`. **Every third-party service is dormant by default**  the system runs end-to-end with zero paid credentials. **AWS Cape Town `af-south-1` migration intentionally deferred** until partnership confirms; turnkey runbook at `docs/AWS_MIGRATION_RUNBOOK.md` covers it as a one-day swap with zero remaining POPIA work to do. Holt forecast + materialised views + external pen-test + nonce-based CSP deferred to launch-scale.)  ✅ done 2026-05-23. See `docs/completed/PHASE_9_COMPLETE.md` and `docs/completed/PHASE_9_PLAN.md`. The system is now launch-ready against the current Neon DB.
- **Phase 9.7** (side-phase between Phase 9 and Phase 10  *nationality analytics & local-hiring intelligence*: 2-class `nationality_class` split + Skills-Shortage Justification Index + Local-Hiring Opportunity Map + employer self-view + `gov`-only per-employer bounded lookup **dormant behind `feature_flag_employer_mix_lookup`** + watch-the-watchers oversight log + printable policy brief. Five structural defences (2-class split, k=10 + complementary suppression, employer floor, dormant-by-default flag, oversight log). Six compliance assertions on `/api/admin/outcomes-compliance` including a structural ban on country-level analytics. Anti-xenophobia tool by construction. **Reframed 2026-05-24** (per operator review): no specific regulatory-mandate or racial-framing claims in user-facing copy  Sebenza is policy intelligence, not a regulatory enforcement tool. DPIA R9 records the reframing as the mitigation.)  ✅ done 2026-05-24. See `docs/completed/PHASE_9_7_COMPLETE.md` (Reframing-decision section) + `docs/completed/PHASE_9_7_PLAN.md`. Seven new query/library files, four new pages, three new API routes, three migrations (`0012`–`0014`), four new nav entries across gov + admin, 22/22 vitest fixtures green, `npm test` + `npm run test:watch` scripts added (vitest devDep is the runner).
- **Phase 9.8** (side-phase between Phase 9.7 and Phase 10  *vacancies & demand-driven matching*: org-private vacancy spec  reverse-match against the talent base  invite specific seekers  accept / decline-with-reason / accept-with-notice / reconsider lifecycle  vacancy→placement linkage  "why roles go unfilled" analytics that unblock 9.7.3's real Local-shortage classifications on production data. **Not a job board** by construction  no public vacancy listing, no apply button, no seeker-side browsing. **No nationality-as-gate on invites** (would contradict Rules 2 + 3 + DPIA R9 mitigation shipped six days earlier)  highlight + honest supply line only, structural defence via compliance assertion (c). Invite expiry is **per-vacancy, employer-set** with a nightly cron + both-sides notifications. Bulk-invite skips non-consented seekers cleanly (soft UX message; per-seeker reason in audit log only, never in UI, to avoid leaking consent state). Decline-note capped at 200 chars with POPIA reminder + PII flag. `accepted_with_notice` is **a yes**, never counted as a decline. `legal_eligibility_note` deferred entirely  no field, no scaffolding. Twelve compliance assertions on the runtime suite (six new in 9.8.8 + six inherited from 7.5 / 9.7). Three additive migrations (`0015` vacancies + lifecycle, `0016` consent_purpose += vacancy_matching, `0017` invitation_state + decline_reason + vacancy_invitations table).)  ✅ done 2026-05-24. See `docs/completed/PHASE_9_8_COMPLETE.md` + `docs/completed/PHASE_9_8_PLAN.md`. Eight tasks shipped; vacancy emails wired end-to-end through the existing Resend path (the earlier "dormant per Phase 8" wording was a missed template-allowlist entry, not the email infra). **Task 9.8.1 (vacancies schema + lifecycle)** ✅ 2026-05-24  `vacancies` table + `vacancy_status` enum + `placements.vacancy_id` FK shipped (migration `0015`); `lib/employer/vacancies.ts` Server Actions with org-scoped reads + bounded state machine + role gating (Owner/Recruiter edit, Viewer read-only); list/new/detail surfaces shipped with mobile-first form, Private salary pill, lifecycle action buttons; eight `vacancy.*` audit kinds reserved (three in use). **Task 9.8.2 (reverse-matching / "Find matches")** ✅ 2026-05-24  `matchVacancyCandidates()` composer reuses `searchProfilesQuery` (Phase 4 ranking SQL  one source of truth, no parallel matcher); new `countMatchesByCitizenship()` query backs the **honest-supply line** *"N SA citizens · M candidates match this vacancy"* (D6 wording) over the full match set, independent of the SEARCH_LIMIT-capped ranked list; `/employer/vacancies/[id]/match` page reuses `<TalentRosterItem>` with the existing `citizen_boost` for highlighting (no new gate  §CRITICAL respected); Find-matches CTA on the detail page is visible to all roles (Viewers can browse the redacted talent pool). **Task 9.8.3 (consent purpose for vacancy invites)** ✅ 2026-05-24  `consentPurpose += vacancy_matching` via additive migration `0016` (same pattern as `0008`), default-off + non-degrading (a non-consenting seeker is still searchable + contactable as today; they just don't receive invites); D8 source text wired verbatim into onboarding (sign-up step 2) + `/dashboard/privacy` with a mobile-first tap-to-expand `<details>` block on phones and an always-visible paragraph on `md+`; new `lib/consent/check.ts` ships `hasConsent()` + `hasVacancyMatchingConsent()` server-only helpers for the 9.8.4 invite action to call at its boundary (structural ban asserted by compliance check (b) in 9.8.8); zu/xh/af stubs continue to fall back to English via the existing deepMerge until pro translation lands. **Task 9.8.4 (invite flow: employer  seeker)** ✅ 2026-05-24  `vacancy_invitations` table + `invitation_state` enum + `decline_reason` enum shipped via migration `0017` with UNIQUE (vacancy_id, profile_id) dedup + (vacancy_id,state) / (profile_id,state) / (expires_at) indexes; `bulkInviteToVacancy` Server Action splits selections through four gates ending in `hasVacancyMatchingConsent()` per D5  per-seeker reason is in the audit log only, never in the response payload (would leak consent state to the employer); soft summary banner verbatim *"N invites sent · M not eligible to receive an invite right now"*; `withdrawInvitation` flips invited→withdrawn + notifies seeker + audits as `vacancy.invite.withdraw`; `/api/cron/vacancy-invite-expiry` shipped with `CRON_SECRET` guard, conditional state flip (idempotent against concurrent seeker responses), fires `vacancy.invite.expired` (seeker, polite) + `vacancy.invite.unanswered` (employer org-wide via `notifyOrgMembers`); cron-only helper kept in non-`"use server"` `lib/employer/invitations-cron.ts` so it can never accidentally become a Server Action invokable by a client component. Three new `NOTIFICATION_CATALOG` kinds (`vacancy.invite`, `vacancy.invite.expired`, `vacancy.invite.unanswered`) with email channel dormant per Phase 8. Match-page client island `BulkInviteIsland` (mobile-first: bottom-sheet modal on phones, centred on `md+`, sticky action bar, select-all/clear chips, "Already invited" pill instead of checkbox for dedup, Viewers see no interactive invite affordances). Vacancy-detail pipeline panel `VacancyInvitationsPanel` shows invitations grouped by tone-coded state pills with per-row withdraw for Owners + Recruiters on `invited` rows. **Phase 9.8 complete (2026-05-24)** — eight tasks shipped across migrations `0015–0017` (vacancies + vacancy_matching consent + invitations); twelve compliance assertions on `/api/admin/outcomes-compliance` (six new in 9.8.8 + six inherited); seed extended with 3 vacancies + 5 invitations spanning every lifecycle state + 3 retroactively-linked BSc CS placements so the vacancyplacement loop has real history immediately. Vacancy emails wired end-to-end through the existing Resend path. See `docs/completed/PHASE_9_8_COMPLETE.md`.
- **Phase 9.9** (pre-launch hygiene side-phase between Phase 9.8 and Phase 10  *experience-in-years on profile & skills*: `profiles.years_experience` + `profile_skills.years_of_experience` integer columns shipped via additive migration `0018`. Self-declared by the seeker; UI clamps 0..60; NULL = "rather not say" (no back-fill from `experiences` history per D1, lossy). Read-side: `/p/[handle]` profession eyebrow reads *"Senior · Chef · 8 yrs"*; skill cards + the right-rail compact list append *"· 5 yrs"* per skill; same on the employer dossier + on `<TalentRosterItem>` (which surfaces on /search + the vacancy match page). 0 displays as *"<1 yr"*; NULL renders unchanged. Public per D4 (CV-header data, not sensitive PII). Phase 4 ranking blend + `minYearsExperience` search filter (Task 9.9.3 OPTIONAL) deferred to post-launch  the plumbing is in place. **DOB + gender explicitly out of scope**  governance-reviewed phase if/when added (the 9.7 nationality_class playbook is the template).)  ✅ done 2026-05-24. See `docs/completed/PHASE_9_9_COMPLETE.md` + `docs/completed/PHASE_9_9_PLAN.md`.
- **Phase 9.10** (last pre-launch side-phase between Phase 9.9 and Phase 10  *employer KYC / org vetting flow*: replaces dormant `feature_flag_kyc_provider` partnership path with admin-mediated vetting. Employer signs up + verifies email  uploads 4 SA-standard KYC docs (CIPC registration certificate, SARS tax clearance, proof of address  3 months, bank confirmation) + optional `other` supporting docs at `/employer/onboarding`  admin reviews at `/admin/verifications` with signed-URL inline document access + 5 actions (Approve / Reject-with-reason / Request-changes-with-note / Resend verification email / Mark-verified break-glass)  approval stamps `verified_at` + `verified_by_user_id`, unlocks every PII-touching surface (`verifyOrgVerified()` was previously redirecting to a static settings page  now redirects to actionable `/employer/onboarding`). 6 new columns on `organizations` + new `organization_documents` table + `org_document_kind` enum via migration `0019`. 7 new audit kinds (`org.submit` / `org.review.{approve,reject,request-changes}` / `org.documents.upload` / `org.verification.resend` / `verification.manual-grant`). 4 new email templates (`org.documents.submitted`, `org.review.changes`, `verification.queued`, `org.rejected`) using the existing `genericTemplate()` shell + Resend. Hardcoded SA-standard 4 docs per D1 (admin-managed CRUD deferred post-launch). `(verified)` route-group file shuffle skipped per D6 deviation  per-page guards from Phase 5 already cover every load-bearing path. 3 lifecycle fixture orgs added to seed (Acme/pending · Globex/rejected · Initech/unverified) alongside Discovery Bank.)  ✅ done 2026-05-25. See `docs/completed/PHASE_9_10_COMPLETE.md` + `docs/completed/PHASE_9_10_PLAN.md`.
- **Phase 9.18** (post-9.17 hygiene cluster shipped same-week  *Remote/Hybrid + email-ops + draft-persistence + auth-UX + domain rename*: bundles one schema add + three platform-ops fixes + three UX polish items into one phase because they shipped same-day and each was small enough that splitting them would have been noise. **(1) Remote + Hybrid on work-availability** via additive migration `0030` (ALTER TYPE … ADD VALUE for the enum). Same picker on sign-up + /dashboard/profile + /search "Available for" facet (data-driven, picks up the new values automatically). Conflates work-mode with employment-type for UX simplicity; documented as a deliberate choice with a clean follow-up path if analytics ever want to split. `work-availability-publicly-safe` compliance assertion's expected-set widened. **(2) Email transport collapsed to a single SMTP path**: drops the Phase 8 dual-transport design (Mailtrap SMTP + Resend SDK) for one nodemailer SMTP path that works against any provider via env vars. Provider swap = config change, not a code change. `resend` npm dependency dropped. `EMAIL_TRANSPORT` shrinks to `smtp | console`; env vars renamed to a single `SMTP_HOST / PORT / SECURE / USER / PASS / FROM / FROM_NAME` set. Trade-off: lose Resend's richer error responses; acceptable at our transactional volume. **(3) Loud-fail diagnostic**: when EMAIL_TRANSPORT is unset in production AND any SMTP_* var is present, `sendEmail()` throws a clear error instead of silently falling back to console (the previous trap that made it look like sends succeeded while no email actually left). Opt-out via `EMAIL_TRANSPORT_STRICT=false`. **(4) Admin test-email panel** on /admin/settings (`<EmailTestPanel>` + new `sendTestEmail` action in `lib/admin/email-debug.ts`): one-click send to the signed-in admin's email confirms the SMTP pipeline reaches the provider; inline panel reports transport + message id (green) / console fallthrough (yellow with env-var fix hint) / raw SMTP error (red). Every test audit-logged. **(5) Form-draft persistence across locale switches** via new `useSessionDraft` hook in `lib/hooks/useSessionDraft.ts`. next-intl's locale switcher swaps the URL (/en/sign-up → /zu/sign-up) and remounts the page tree which previously wiped every useState in a form; the hook persists to sessionStorage on every state change, restores on mount, clears after successful submit. Applied to 6 forms: SeekerSignUpForm, EmployerSignUpForm, VacancyForm (per-vacancy `draftId` scoping so drafts don't bleed across edit-A → edit-B), OrgOnboardingForm (text fields only — uploaded docs are already server-side), ProfileBasicsForm, SkillsEditor (with taxonomy revalidation on restore). EmployerSignUpForm required converting uncontrolled FormData inputs to controlled state to make persistence work. **Three invariants**: passwords/blobs/secrets never written; restoration runs in useEffect not initial useState (no hydration mismatch); all storage errors silent (private browsing / quota). **(6) Password show/hide eye toggle** via new `<PasswordField>` client component wrapping the existing FieldShell + input styling. Lives in its own file so `TextField` stays server-friendly. Swapped into SignInForm + SeekerSignUpForm (2 fields) + EmployerSignUpForm (2 fields) + ResetPasswordForm (2 fields). Toggle is tabIndex=-1 so Tab from the input goes to the next form field — keyboard users rarely need it, accidental password reveal on a shared screen is the bigger harm. **(7) Domain rename**: sebenza.co.za → sebenzasa.com across 20 files (codebase had the old domain hardcoded from initial scaffolding; sebenzasa.com is the actual production domain since sebenza.com wasn't available). Brand name "Sebenza" preserved everywhere — only the URL host + email-address domain changed. Migration `0030_phase9_18_remote_hybrid.sql` is the only DB change. Compliance suite still at 29 assertions. Typecheck + 50/50 Vitest + production build all green.)  ✅ done 2026-05-28. See `docs/ROADMAP.md` Phase 9.18 section.
- **Phase 9.17** (post-9.16 agent-workflow gap closure  *employer-initiated seeker invites*: closes the SA staffing-agency workflow gap (agents maintain rosters on WhatsApp + Excel + bring people to platforms via "go sign up" links). Verified-org employer sends a single-invite-at-a-time onboarding nudge (email + optional name + optional profession + 200-char personal note); recipient lands on `/sign-up/invited/[token]` with name + profession pre-filled and email locked, completes a customised `<SeekerSignUpForm>` via the new `acceptSeekerInvitation` server action; the inviter sees them appear on `/employer/invites` Joined list. **No vacancy coupling** (D3)  Phase 9.8 vacancy invitations handle role-specific outreach once the seeker's on the platform. **No special PII access** on joined seekers (D9)  the inviter sees what every verified employer sees, no more. Three states surfaced: Pending (Withdraw + Resend actions), Joined (link to public profile), Declined (with 90-day POPIA §11 cooldown badge). POPIA defences end-to-end: verified-org gate via `verifyOrgVerified()` (D1); **transparent dedupe** when email already has Sebenza account (D4) backed by 50/day per-org rate-limit counting dedupe-hits + cooldown blocks alongside successful sends (D7.1) so brute-enumeration is throttled regardless of path; 90-day per-(org, email) decline cooldown (D7.2); 500/day platform cap (D7.3); §16 transparency via token-gated `/report-invite/[token]` flow that needs no auth + flags admins; PII flag on the 200-char personal note in audit log meta (D6); signed HMAC-SHA256 tokens with their own env var `SEBENZA_INVITE_SIGNING_SECRET` (D8)  14-day expiry, single-use at the DB layer, 6 Vitest fixtures lock the contract. Migration `0029_phase9_17_seeker_invitations.sql` (additive) adds `seeker_invitations` table + `seeker_invitation_state` enum + 3 indexes. 6 new audit kinds (`org.seeker_invite.{send,accept,decline,withdraw,expire,reported}`) + 2 new notification kinds (`org.seeker_invite.accepted` audience org_members for the inviter side, `org.seeker_invite.reported` audience all_admins for abuse oversight  both in-app default-on, email default-off). 3 new compliance assertions on `/api/admin/outcomes-compliance` (now **29 total**): `seeker-invite-verified-org-only` + `seeker-invite-cooldown-honoured` + `seeker-invite-no-orphan-when-user-exists`. New `/employer/invites` route + `UserPlus` nav entry between Vacancies and Talent pools. New cron `/api/cron/seeker-invite-expiry` (CRON_SECRET-guarded). `<SeekerSignUpForm>` extended with `invitationContext` prop. `<ComboboxField>` reused for profession picker on both invite form + landing page. Recipient-facing email in `lib/email/templates/seeker-invite.ts` reuses the shared `emailShell()` brand chrome with a POPIA §16 footer. Seed: 3 demo invites on Discovery Bank (pending + accepted + declined inside cooldown)  required flipping Discovery from `unverified` to `verified` in the seed since the Phase 9.17 gate gates on it; the existing Phase 9.10 fixtures (Acme/Globex/Initech) still cover gate-testing dev workflows. DPIA addendum R11 + Privacy Section 2 + PAIA Section 4 updated.)  ✅ done 2026-05-27. See `docs/PHASE_9_17_PLAN.md`.
- **Phase 9.16** (post-9.15 identity-capture broadening  *DOB + nationality at sign-up + admin-mediated ID document verification*: started as three coupled gaps (DOB / passport / KYC review); a same-day operator review (2026-05-27) trimmed the sign-up form to two captures and moved ID number + document entirely to `/dashboard/profile`. Final shape: **(1) DOB at sign-up** via new reusable `<DatePicker>` (3-view popover: day → month → year, mobile bottom-sheet, range-clamped). Server-side 14100 age gate via `validateDob` in `lib/auth/id-validation.ts` (lower bound = SA Basic Conditions of Employment Act minimum). DOB editable from `/dashboard/profile` via `<DateOfBirthEditor>` and **never** included in public projections  guarded by new `dob-never-in-public-payload` compliance assertion. **(2) Nationality at sign-up (post-trim)** via searchable `<ComboboxField>` populated from a new static `lib/taxonomy/countries.ts` catalogue (~191 ISO 3166-1 alpha-2 codes, SA + SADC pinned at the head). Each option renders with a flag emoji in the new `leading` slot on `ComboboxOption` (Unicode regional-indicator pair, no image assets, no font dependency). Flag glyph is **excluded from the type-to-filter rank** so typing "south" still ranks "South Africa" at idx 0 rather than after the flag. Server action persists `profiles.nationality` (country label) + derives `profiles.is_citizen = (code === "ZA")` for the Phase 9.7 2-class `nationality_class` analytics + Citizen-Visibility Rule highlight. **(3) Passport support (column / KYC path only)** via new `id_document_kind` pgEnum (`sa_id` | `passport`, default `sa_id`) + `passport_country` text column. The user picks SA ID or passport on `/dashboard/profile` when they add their document; SA ID flows through 13-digit + Luhn + DOB-prefix cross-check, passport through 6-20 alphanumeric + ISO-3166 issuer check. `national_id_enc` column retained kind-agnostic (D1: renaming would touch too many sites for zero benefit). `passport-country-when-passport` compliance assertion enforces issuer presence + validity. **(4) Admin-mediated ID document verification** since the KYC-SaaS partnership never landed: seekers upload from the KYC panel; admins review from new "Seeker IDs" tab on `/admin/verifications` with three actions (Approve / Reject / Request changes)  mirrors Phase 9.10 org-vetting shape 1:1. `<KycPanel>` rewritten as 5-state lifecycle (verified / no-ID / rejected / pending review / needs upload). New `uploadIdDocument` Server Action with magic-byte sniff + 10MB cap + 5-uploads/10-min rate limit (path convention `{userId}/id-documents/{profileId}.{ext}`  owner-prefixed, enforced by `kyc-document-private` compliance assertion). **(5) Admin toggle to hide verification badges platform-wide** via new `feature_flag_verification_badges_visible` setting on `/admin/settings` (default ON). When OFF, both the badge pill and the Avatar verification ring disappear across every seeker-facing surface  useful as a transitional knob while SAQA + KYC verification volume is still thin; Verification-Honesty Rule intact (column + roll-up unchanged, we just don't paint). 3 new audit kinds (`kyc.document.upload`, `kyc.review.approve`, `kyc.review.reject`) + 2 new notification kinds (`kyc.approved`, `kyc.rejected`, audience seeker, in-app default-on / email default-off, no dedupe). 4 new compliance assertions on `/api/admin/outcomes-compliance` (now **26 total**): dob-never-in-public-payload + id-encryption-mandatory (verifies every non-null `national_id_enc` carries the `v1.` AES-256-GCM prefix) + passport-country-when-passport + kyc-document-private. Migration `0028_phase9_16_dob_passport_kyc_upload.sql` (additive) adds 6 columns + the enum + a partial index on the pending-review queue. 22 Vitest fixtures for the validation helpers (DOB boundaries + SA ID Luhn + DOB cross-check + whitespace tolerance + passport regex + country lookup). Full suite at 44 passing. DPIA addendum R10 + Privacy Section 2 + PAIA Section 4 updated. Defence-in-depth on `signUpSeeker`: every client-side validator re-runs server-side before the row is written.)  ✅ done 2026-05-27. See `docs/PHASE_9_16_PLAN.md`.
- **Phase 9.15** (post-9.14 user-facing gap closure  *"Other" free-text + admin taxonomy suggestion queue*: real users with unusual professions or non-canonical institutions can now onboard via "Other (specify)" footer on the sign-up combobox + admins get a structured queue at `/admin/taxonomy/suggestions`. One mechanism covers both professions + institutions, discriminated by a `kind` enum (locked in D0). Additive migration `0024` ships: new `taxonomy_suggestions` table (kind + state enums + CHECK 2-80 char invariant + 2 indexes); new `institutions.is_pending` boolean + `institutions.deleted_at` timestamp for the pending-institution flow. `<ComboboxField>` extended with `allowOther` + `onOtherSubmit` + `otherLabel`  picking the footer transforms the field into a free-text input with a "Pick from list" revert. Five Server Actions in `lib/taxonomy/suggestions.ts`: submit (validates + rate-limits 5/user/day + creates suggestion + for institution kind creates pending row + notifies admins with `(kind, lower(custom_text))` dedupe), listPending (dedupes by customText with submitter-count aggregation), promote (canonicalises + backfills profiles/academic_profiles + de-dupes pending rows), merge (links to existing + backfills + deletes pending row), reject (state='rejected', **user data NEVER mutated** per Verification-Honesty Rule + D4). Sign-up flow wired: `signUpSeeker` resolves institution_slug pre-transaction (creates pending row if free-text) + fires post-transaction suggestion submissions for custom profession + institution values; auxiliary so never blocks the user's signup. New admin queue page `/admin/taxonomy/suggestions` (+ banner on `/admin/taxonomy` with pending count); three inline-form actions per card (Promote with optional spelling correction / Merge via combobox over canonical / Reject with reason). 4 new audit kinds (`taxonomy.suggestion.{submit,promote,merge,reject}`); 1 new notification kind (`taxonomy.suggestion.received`, audience all_admins, in-app default-on + email default-off, 24h `(kind, custom_text)` dedupe); 3 new compliance assertions wired to `/api/admin/outcomes-compliance` (now 22 total): suggestions-valid (CHECK backup), rejected-preserve-data (user data preservation contract), backfill-complete. Seed extended with 3 demo rows (pending "Game Ranger" + pending "Damelin College" with matching pending institutions row + rejected "asdfasdf" with full audit trail). **Deferred to follow-ups**: `allowOther` on profile editor + vacancy form (slug-vs-label state divergence requires focused refactor); full institution admin CRUD UI (schema in place, queue covers immediate path).)  ✅ done 2026-05-26. See `docs/completed/PHASE_9_15_COMPLETE.md` + `docs/completed/PHASE_9_15_PLAN.md`.
- **Phase 9.14** (post-9.13 surgical fix to close a structural gap surfaced during system review  *seeker profile verification roll-up*: `profiles.verification` was scaffolded but never wired to any code path, so every seeker rendered as "Unverified" regardless of qualification state. Fix derives the column from `qualifications.verification`: verified ⇔ ≥1 qualification verified; pending ⇔ no verified, but ≥1 pending; unverified otherwise. `rejected` is never auto-applied at profile level per the Verification-Honesty Rule (per-document rejection isn't a per-seeker judgement). New helper `recomputeProfileVerification()` in `lib/profile/verification-rollup.ts` is the only writer; wired into four mutation sites: `approveQualification` + `rejectQualification` (admin) + `uploadQualificationDocument` + `deleteQualification` (seeker). `addQualification` deliberately NOT wired since a new qual starts `unverified` and can never elevate. Admin approve/reject audit rows extended with `profileVerificationChanged/From/To` meta. Migration `0022_phase9_14_profile_verification_backfill.sql` (additive, data-only, idempotent) runs the same roll-up against every non-deleted profile so existing rows converge to honest state without a re-seed. New compliance assertion `profile-verification-matches-rollup` wired into `/api/admin/outcomes-compliance` (now 19 total) as a structural pin against regressions.)  ✅ done 2026-05-26. See `docs/completed/PHASE_9_14_COMPLETE.md`.
- **Phase 9.13** (sister phase to 9.12; last side-phase before Phase 10  *learning-loop intelligence*: closes the pre-launch gov analytics chapter end-to-end. Two new datasets: (1) **curriculum vs demand** via new migration `0021` + `programme_skills` table (hand-curated D4 mapping, admits future SAQA-feed expansion; 44 seeded rows across 8 institutions × 5 programme archetypes). `demandVsCurriculumQuery` joins `programme_skills` × 90d demand from `search_events` × `institutions.province_slug`; returns covered skills + the top in-demand gap skills the programme doesn't cover. Student-side `<ProgrammeVsMarketCard>` on `/dashboard/grow` (focused view of seeker's own programme, no suppression) + gov-side `/gov/curriculum` (new route + new `GraduationCap` nav entry, cross-market, k=10 + two complementary suppression axes, CSV export `/api/gov/curriculum/export`). (2) **Why learners stall** — `stallReasonAggregateQuery` aggregates 9.12's `learning_items.abandon_reason` rows by `(skill × province × reason)`. Three structural defences: D1 `outcomes_research` INNER JOIN consent gate (unconsented learners structurally excluded before suppression), D5 NO provider dimension (provider judgment is reputational, not policy intel — locked structurally by aggregating only on skill/province/reason), D6 freshness-weighted via the same `sebenza_freshness_confidence(abandoned_at)` SQL function 9.7 + 9.8.7 use. `<StallReasonsCard>` extends `/gov/shortage` below the existing decline-reasons card — mirrors the 9.8.7 `<DeclineReasonsCard>` visual idiom verbatim. CSV at `/api/gov/stall-reasons/export`. Both cards carry footer cross-references explaining how to read them together with the Justification Index (three different interventions land on the same skill gap depending on whether it's supply-driven / salary-driven / cost-driven / quality-driven). Three new compliance assertions on `/api/admin/outcomes-compliance` (now 18 total): `curriculum-cells-above-floor`, `stall-cells-above-floor`, `stall-consent-gate-enforced` (the last verifies the INNER JOIN gate by counting the consented-source rows and confirming the query result's sum-of-counts never exceeds them — if the gate regresses, this assertion fires). Seed extended with 10 abandoned `learning_items` on `postgres` for the BSc CS cohort so the stall-cell infrastructure has demonstrable suppressed counts at ship time. No new audit kinds (reuses `analytics.export`). Known compromise: `programme_skills.programme` is free text matching the exact `academic_profiles.programme` string — a `programme_aliases` view is the future polish. Typecheck clean.)  ✅ done 2026-05-25. See `docs/completed/PHASE_9_13_COMPLETE.md` + `docs/completed/PHASE_9_13_PLAN.md`.
- **Phase 9.12** (side-phase between 9.11 and 9.13  *the learning loop*: turns the Career Compass from advice-you-read into a loop the learner lives in. Additive migration `0020` adds the provenance honesty contract on `profile_skills` (`provenance skill_provenance` enum + `verified_at` timestamp; D1 UI rule: render "Verified" ⇔ `provenance='verified_provider'` AND `verified_at IS NOT NULL`) + a new `learning_items` table with `learning_state` + `abandon_reason` enums. Four Server Actions own the state machine (`acceptRecommendation` → `accepted`; `startLearningItem` → `in_progress`; `completeLearningItem` → `completed` + upsert `profile_skills` with **upgrade-only** semantics + fire `learning.completed` celebration notification; `abandonLearningItem` → `abandoned` + reason + clamped 200-char note PII-flagged in audit). New "My Learning" section on `/dashboard/grow` (server component, splits Active / Recent) + `<AcceptRecommendationButton>` on every recommendation card (CTA or "On your learning list" pill) + `<AbandonModal>` (bottom-sheet on phones / centred on `md+`, mirrors 9.8.5 decline UX). D3 free-alt surfacing: cost/access-driven abandons (`too_expensive` / `access_transport`) light up a dashed "Free alt" chip on the matching recommendation next render. D4 strong-lean honored: no streaks, points, or leaderboards — Talent-Pulse ring + state pill + projected-rank delta only. D5 cross-kind weekly cap enforced in the new `/api/cron/learning-nudge` cron: before queuing each `learning.nudge`, the cron checks the `notifications` table for any `vacancy.outcome.other-hired` OR `learning.nudge` row in the last 7 days for that user — if either exists, skip. Channel-agnostic. `learning.completed` exempt (positive payoff never throttled). Three new compliance assertions wired into `/api/admin/outcomes-compliance`. Seed extended with 3 learning fixtures on `wits-bsc-cs-2026-08` (react in-progress, typescript completed → `profile_skills` upgraded to `self_attested_learning`, postgres abandoned with reason `too_expensive`) so 9.13's stall analytics has real data to aggregate against from day 1.)  ✅ done 2026-05-25. See `docs/completed/PHASE_9_12_COMPLETE.md` + `docs/completed/PHASE_9_12_PLAN.md`. Next sister phase: `docs/PHASE_9_13_PLAN.md`.
- **Phase 9.11** (side-phase between Phase 9.10 and Phase 10  *vacancy-outcome loop + growth notifications*: closes two pre-launch hygiene gaps. (1) Mark-as-Filled now captures who was hired in the same atomic action: lifecycle button on `/employer/vacancies/[id]` opens `<MarkAsFilledModal>` (bottom-sheet on phones, centred on `md+`) which forces ≥1 hire pick or an explicit Skip — `markVacancyFilledAndLogHires` wraps placement inserts + state flip + outcome fan-out in `db.transaction()`; race-protected (second attempt on `filled` refused); multi-hire batches share hire-date + salary; per-row `placement.confirmed` notification preserves the existing seeker self-confirmation flow (Placement-Truth). (2) Candidates who weren't selected get honest closure: every accepted invitee minus hires/declined/expired/withdrawn/reconsidering/still-pending (D5) gets `vacancy.outcome.other-hired` notification + email composed against the **vacancy requirements** — *never* the hired person's profile (D4 privacy invariant); cites role's skills, recipient's overlap + gaps, optionally the 9.8.7 dominant decline-reason (k-floor suppressed), deep-links to `/dashboard/grow?missing=<slugs>`. Career Compass reads the param, renders a top banner restating the role's gaps + highlights matching recommendation rows with a "Vacancy gap" chip. Outside-pipeline typeahead via `searchOutsideHireCandidates` (org-verified, province-scoped, excludes existing invitees, audit-logged); fan-out capped at 100 to protect rate limit. 4 new audit kinds (`org.vacancy.filled.batch`, `org.vacancy.filled.no-placement`, `search.outside-hire-lookup`, `vacancy.outcome.other-hired`); 1 new notification kind (audience seeker, in-app + email default-ON); 1 new email template (`genericTemplate` with eyebrow "Vacancy outcome" + CTA "Open Career Compass"). 2 new files (`lib/seeker/vacancy-outcome.ts` pure helper + `MarkAsFilledModal` client island); 3 Server Actions on `lib/employer/vacancies.ts`. No new migration.)  ✅ done 2026-05-25. See `docs/completed/PHASE_9_11_COMPLETE.md` + `docs/completed/PHASE_9_11_PLAN.md`.
- **Phase 10** (dual arc: help centres 10.1–10.4 ✅ shipped 2026-05-29; public launch 10.5–10.11 code-side prep ✅ landed 2026-05-30, operator hands remain). **Arc A** shipped 108 hand-written help articles across four role-specific centres (employer / seeker / admin / gov) + ~80 in-context `HelpLink` chips across 33 dashboard surfaces. **Arc B** (renumbered 10.5–10.11 from the original 10.1–10.7 to avoid colliding with Arc A): WCAG 2.2 AA audit static-scan landed (`docs/A11Y_AUDIT.md`); Lighthouse CI config landed (`lighthouserc.json` + `lighthouse-budgets.json`); Tier-2 + Tier-3 locale stub catalogs landed (`messages/{nso,tn,st,ts,ve,ss,nr,pt,fr,sw}.json` with `__notice` markers); `next.config.ts` gates `@next/bundle-analyzer` behind `ANALYZE=true`. Operator-hands remaining: live-credentials flip (Resend / Sentry / Upstash / KYC / SAQA env vars), optional AWS Cape Town `af-south-1` migration (runbook ready), soak + golden-path E2E, manual screen-reader passes, throttled-device Lighthouse runs, professional human translation. See `docs/PHASE_10_LAUNCH_PLAN.md`. No new features in Phase 10 — feature requests collect in `docs/POST_LAUNCH_BACKLOG.md` for Phase 11+.

- **Hiring UX polish** ✅ shipped 2026-05-30 (post-help-centre, pre-Phase-11): lifeguard / pool-attendant / swim-instructor professions added; **profession "Other"** affordance on `ProfileBasicsForm` + `VacancyForm` with admin promotion via `/admin/taxonomy/suggestions`; seasonal window gains optional anchor-year fields; **skill chip-toggle replaced with typeahead `MultiSelectComboboxField`** + profession-scoped suggestion ranking (`PROFESSION_SKILLS_MAP`); SKILLS taxonomy expanded from 15 to ~50 entries; **skill "Other"** path added (kind='skill' in `taxonomy_suggestion_kind` enum); admin skill-suggestion Promote / Merge / Reject wired; **`MonthYearPicker` consolidation** across vacancy seasonal window + current employment + experience rows.

- **Phase 11.1** (engagement velocity)  ✅ shipped 2026-05-30. Six tasks: weekly digest email + Monday cron + new `seeker.weekly_digest` notification kind + bespoke email template (11.1.1); "why no invites?" diagnostic card on the empty inbox (11.1.2); welcome-back delta card on the dashboard with a cookie-tracked 7-day absence window (11.1.3); achievement badges + nightly sweep cron + six SVG medallions in `public/badges/` + recent-achievements strip + new `achievement.awarded` audit kind + `seeker_badges` migration `0038` (11.1.4); invitation urgency chip on `/dashboard/invitations` cards within 48 h (11.1.5); audit-log link prominence on the dashboard when weekly viewers > 0 (11.1.6). No new consent purposes, no new data category beyond `seeker_badges`. See `docs/completed/PHASE_11_1_COMPLETE.md` + `docs/completed/PHASE_11_1_PLAN.md`.

- **Phase 11.2** (learning loop completion)  ✅ shipped 2026-05-30. Nine tasks. **(11.2.1)** `LearningPath.url` + `sebenzaReviewed` editorial chip + new `learning_path.opened` audit kind + outbound-CTA client island (D1: direct deep-link, no Sebenza redirect). All 5 seeded paths backfilled with verified URLs. **(11.2.2)** Free-alternative inline on cost-driven abandonment: new `lib/seeker/free-alternatives.ts` with deterministic ranking (free > subsidised > national > shortest), new `fetchFreeAlternativeForItem` + atomic `swapToFreeAlternative` Server Actions, new `learning.swapped_to_free` audit kind + notification kind. AbandonModal rewired to surface the alternative inline. **(11.2.3)** Completion -> cert-upload bridge: secondary CTA on completed `<LearningItemRow>` rows; `/dashboard/qualifications` accepts `prefillTitle` + `prefillInstitution` query params; QualificationsManager auto-opens Add panel with prefill. D3 honored: no FK linking cert to learning_item. **(11.2.4)** Parking-lot `interested` state: additive migration `0039` (`ALTER TYPE learning_state ADD VALUE 'interested' BEFORE 'accepted'`); new `markLearningInterested` + `promoteInterestedToPlanned` actions + 2 audit kinds; "Save for later" secondary CTA on `<AcceptRecommendationButton>`; "Saved for later" sub-section on `<MyLearningSection>` above active items. **(11.2.5)** `<SkillJourneyTimeline>` component renders completed items chronologically; silent when empty. **(11.2.6)** City-demand table rows become `<Link>`s to `/search?q=...&province=...` (D5: public search, not employer-restricted). **(11.2.7)** `updateSkills` now revalidates `/dashboard/grow`; `completeLearningItem` extends revalidation to `/dashboard`. Closes the stale-recommendations correctness gap. **(11.2.8)** Adjacent profession pivot CTA: new `switchPrimaryProfession` Server Action (single-field, audits with `pivot: true` meta); new `<SwitchProfessionConfirmModal>` (bottom-sheet/centred); `<AdjacentProfessionSwitch>` client wrapper keeps the card a server component. **(11.2.9)** New `<StudentLaneDiscoveryCallout>` (full + compact variants) renders for seekers without `me.academic`; wired into `/dashboard` (full) + `/dashboard/grow` (compact). Silently disappears once an academic record exists. No new tables; 4 new audit kinds; 1 new notification kind. See `docs/completed/PHASE_11_2_COMPLETE.md` + `docs/completed/PHASE_11_2_PLAN.md`.

- **Next:** Phase 11.3 (seeker control + trust posture). Then 11.4 (SA distribution surface), 11.5 (profile depth + mobile / a11y polish). Founder priority: 2 weeks = 11.1 ✅ + 11.2 ✅ + trust trio from 11.3; 6 weeks = full Phase 11. See `docs/PHASE_11_PLAN.md` + `PHASE_11_{3,4,5}_PLAN.md`. **Phase 12** (renumbered from old Phase 11) = Testing & QA.

Outstanding-but-not-blocking from 9.7: `feature_flag_employer_mix_lookup` flip if/when a concrete operational need lands; cron + email distribution of `/gov/brief` as the recurring artefact; real "Local shortage" classifications emerge as decline-reason data flows in (9.8.7).

---

# TECHNICAL STACK
- **Framework:** Next.js 16.2.6 (App Router, **no `src` dir**, React 19 Server Components + Server Actions, Turbopack).
- **Language:** TypeScript (strict, `noUncheckedIndexedAccess`).
- **Styling:** Tailwind CSS v4 (design tokens in `app/globals.css` via `@theme`). **No Framer Motion**: animation is CSS-only and purposeful (count-up on insights, chevron draw-in, hero reveal).
- **Icons:** Lucide React.
- **State:** Server Components + Server Actions; React `useState`/`useTransition` for in-component UI state; TanStack Query reserved for the interactive search surface in Phase 4 only.
- **Database:** Neon Postgres + Drizzle ORM (`drizzle-orm` 0.45 + `drizzle-kit` 0.31 + `drizzle-zod`).
  - **Hosting path:** Neon (`eu-central-1`) for Phase 2 → migrate to self-hosted Postgres
    in AWS Cape Town (`af-south-1`) on Docker in Phase 9, so PII never leaves SA
    jurisdiction. Drizzle is driver-agnostic; the swap is `db/client.ts` only.
    Schema, queries, seed script don't change.
- **Auth:** Better Auth 1.6.11 (Drizzle adapter; email + password + email verification + forgot/reset). 2FA enforcement deferred to Phase 7 task 7.2.
- **Validation:** Zod (single source of truth via `drizzle-zod`).
- **File storage:** **Supabase Storage** (private bucket, server-side service-role key, signed URLs only) for CVs / certificates / profile photos. We use Supabase Storage standalone  auth is Better Auth, DB is Neon.
- **Email:** env-driven transport (`lib/email/send.ts`)  Mailtrap for dev/staging via `nodemailer`, Resend SDK for production, console fallback. Wired into Better Auth's verification + password-reset callbacks in Phase 2.
- **i18n:** `next-intl` 4.12 (App Router, `app/[locale]/…` routing, ICU message format). Human-translated catalogs; never machine-translate consent / legal copy. Tier-1 locales `en` / `zu` / `xh` / `af`.
- **Search:** Postgres FTS (`tsvector`) + `pg_trgm`. NO external search engine. Phase 4.
- **Charts:** Recharts 3.8 (mount-gated client island to dodge SSR sizing).
- **Rate limiting:** Upstash Redis (auth + search endpoints). Phase 9.
- **Fonts:** Fraunces (display, variable, optical sizing) + Hanken Grotesk (body, variable). Both subset latin, `font-display: swap`, served by `next/font`.

---

# DOMAIN & COMPLIANCE RULES (NON-NEGOTIABLE)
1. **No-Flash Rule:** Performance and accessibility beat aesthetics. Every page must be usable on a
   low-end Android phone over 3G. No 3D, no heavy animation, no large hero media. Data-light by default.
2. **Location-Not-Nationality Rule:** Talent is filtered by **place of residence/work + skill**, NEVER
   gated by nationality. Nationality is a *displayed, optionally-filterable* attribute  never a barrier.
   A foreigner legally resident in Cape Town appears in Cape Town searches.
3. **Citizen-Visibility Rule:** SA citizens may be ranked/highlighted, but the platform is inclusive of
   legally-resident foreign nationals. Framing is "match talent," never "exclude foreigners."
4. **POPIA-First Rule:** This is a special-category PII system (ID numbers, qualifications). Consent,
   encryption, audit logging, and right-to-erasure are built in from commit one  never retrofitted.
5. **Redaction Rule:** Public/search payloads NEVER include ID numbers, documents, or raw contact
   details. These are revealed only to verified employers, post-consent, and every access is audit-logged.
6. **Verification-Honesty Rule:** Never display "Verified" for self-reported data. Default is
   `unverified`. Badges must reflect reality (`unverified / pending / verified / rejected`).
7. **Status-Freshness Rule:** Employment status is time-aware (`statusConfirmedAt`). Stale statuses are
   down-ranked and nudged for re-confirmation. Analytics must distinguish fresh from stale data.
8. **Placement-Truth Rule:** A hire is only counted in analytics when logged/confirmed via the platform.
   Self-reported employment status ≠ a confirmed placement.

---

# CRITICAL UX RULES
1. **Search-First:** The core experience is "search [profession] in [location] → trustworthy results."
   It must be instant, obvious, and work with zero onboarding.
2. **Mobile-First & Low-Data:** Design for a 360px screen and a slow connection first; desktop second.
3. **Protected Routes:** No employer accesses talent contact/documents without `auth` AND `orgVerified`.
   No route touches PII without role check + audit log.
4. **Trust Signals Everywhere:** Verification badges, status freshness, and profile completeness must be
   visible and honest. Trust is the product.
5. **Accessible by Default:** WCAG 2.2 AA. Keyboard nav, contrast, screen-reader labels  non-optional
   for a public-good platform.
6. **Interactive Feedback:** Every action has lightweight feedback (spinner, toast, inline error). Keep it cheap.
7. **Plain, Multilingual Language:** Copy is simple and translation-ready (every string in i18n catalogs,
   no hardcoded text). **Launch (Tier 1):** English (base), isiZulu, isiXhosa, Afrikaans. Structured for the
   full official set (see `ROADMAP.md` Phase 10). Consent / POPIA / legal copy must be **professionally
   human-translated**, never machine-translated.

---

# YOUR GOAL
I will give you a Phase from `ROADMAP.md`. You will architect and code the components/logic for it,
respecting every rule above. Prioritise **correctness, data integrity, POPIA compliance, performance,
and accessibility** over visual flourish. When a "wow factor" instinct conflicts with the No-Flash Rule
or the POPIA-First Rule, the rule wins  every time.
